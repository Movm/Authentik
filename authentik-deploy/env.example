# Authentik Environment Variables für Coolify
# Diese Variablen müssen in Coolify unter "Environment Variables" gesetzt werden

# PostgreSQL Konfiguration
SERVICE_USER_POSTGRESQL=authentik
SERVICE_PASSWORD_POSTGRESQL=your-secure-password-here
POSTGRES_DB=authentik

# Redis Konfiguration  
REDIS_HOST=redis

# Authentik Secret Key (64 Zeichen, zufällig generiert)
SERVICE_PASSWORD_64_AUTHENTIKSERVER=your-64-character-secret-key-here

# Authentik Tag Version
AUTHENTIK_TAG=2025.2.3

# Email Konfiguration (optional)
AUTHENTIK_EMAIL__HOST=localhost
AUTHENTIK_EMAIL__PORT=25
AUTHENTIK_EMAIL__USERNAME=
AUTHENTIK_EMAIL__PASSWORD=
AUTHENTIK_EMAIL__USE_TLS=false
AUTHENTIK_EMAIL__USE_SSL=false
AUTHENTIK_EMAIL__TIMEOUT=10
AUTHENTIK_EMAIL__FROM=authentik@yourdomain.com

# Error Reporting
AUTHENTIK_ERROR_REPORTING__ENABLED=false

# Domain Configuration
SERVICE_FQDN_AUTHENTIKSERVER_9000=your-domain.com

# Wichtige Hinweise für Coolify:
# 1. Setzen Sie diese Variablen in Coolify unter "Environment Variables"
# 2. Generieren Sie einen sicheren 64-Zeichen Secret Key mit: openssl rand -base64 64
# 3. Der Service läuft auf Port 9000 - stellen Sie sicher, dass dieser in Coolify konfiguriert ist
# 4. Volumes werden automatisch erstellt, achten Sie auf Berechtigungen